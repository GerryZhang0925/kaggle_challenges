# url
`
[Distance to run prediction](https://signate.jp/competitions/984)

# set up environment
```
conda create -y --force -n ag python=3.8 pip
conda activate ag
pip install "mxnet<2.0.0"
pip install autogluon
```
